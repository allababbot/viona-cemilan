# Cloudflare Pages Build Configuration

# Build command
build.command = "npm run build"

# Output directory
build.publish = "dist"

# Environment variables (if needed)
[build.environment]
NODE_VERSION = "18"
